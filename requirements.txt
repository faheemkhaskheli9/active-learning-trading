# Runtime dependencies, pinned as implementation progresses.
#
# Phase 1 (baseline model + daily prediction pipeline):
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
lightgbm>=4.0
joblib>=1.3
PyYAML>=6.0
psycopg[binary]>=3.1   # predictions table storage (storage.PostgresPredictionStore)

# Phase 3 adds mlflow and a scheduler.
